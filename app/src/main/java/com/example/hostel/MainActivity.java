package com.example.hostel;

import androidx.appcompat.app.AppCompatActivity;

import android.os.Bundle;

public class MainActivity extends AppCompatActivity {//app's main activity 

    @Override
    protected void onCreate(Bundle savedInstanceState) {//onCreate function which executes when the app loads
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_main);
    }
}
